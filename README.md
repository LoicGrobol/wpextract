Wiki Extract
============

Extract links from wikicode.

## How to use this?

Install dependencies

```bash
uv pip install -U -r requirements.lst
```

Run the thing (assuming `local/page.wiki` contains wikicode)

```bash
python cli.py local/page.wiki
```
